# Get metadata for all datasets in marea

Returns a summary data frame including region, time span, and source
information for each dataset.

## Usage

``` r
marea_metadata()
```

## Value

A data.frame with columns: Dataset, Region, TimeSpan, Source
